{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "outDir": "dist",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "types": ["node", "jest"],
    "strict": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*", "tests/**/*"]
}

//encountering an error in your tsconfig.json because of a mismatch between the 
// configured "rootDir" and the files included in the build.
//But you're also including files from "tests/**/*", which are outside the src directory.
// TypeScript expects everything to be inside rootDir.